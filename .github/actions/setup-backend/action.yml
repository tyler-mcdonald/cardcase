name: Setup backend
description: Install backend dependencies with uv

runs:
  using: composite
  steps:
    - uses: astral-sh/setup-uv@v10.0.1
      with:
        python-version: "3.13"
        enable-cache: true
    - run: uv sync --locked
      shell: bash
      working-directory: backend
