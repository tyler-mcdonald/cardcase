[project]
name = "backend"
version = "0.1.0"
description = "Shared backend for Card Case"
requires-python = ">=3.13"
dependencies = [
    "django>=6.1,<7.0",
]

[dependency-groups]
dev = [
    "django-stubs>=6.1.0,<6.2.0",
    "ruff>=0.16.3,<0.16.4",
]
